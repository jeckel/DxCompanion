ComposerScriptButton {
  width: auto;
}

TabPane {
  padding: 0 0;
  margin: 0 0;
}

DockerContainer {
    Horizontal {
        height: 3;
        padding: 0 1;
        Label {
          padding: 1 1;
        }
    }
    ContainerLogWidget {
        border-title-color: $accent;
        border: $primary-background round;
    }
}
